pipeline {
    agent any
    stages {
        stage('branch-a Branch') {
            steps {
                echo 'Running pipeline from branch-a branch '
            }
        }
        stage('Build A') {
            steps {
                echo 'Running Pipeline for Branch A'
            }
        }
    }
}
